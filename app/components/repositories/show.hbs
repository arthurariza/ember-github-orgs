<fieldset data-test-repository-repo>
  {{@repository.name}}
  {{@repository.html_url}}
  {{@repository.language}}
  {{@repository.private}}

  <LinkTo
    @route='organizations.repositories.branches'
    @model={{@repository.name}}
  >
    Show Branches
  </LinkTo>
</fieldset>
