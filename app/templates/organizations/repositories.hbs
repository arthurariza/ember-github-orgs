{{page-title 'Repositories'}}

{{#each @model as |repo|}}
  <fieldset data-test-repository-repo>
    {{repo.name}}
    {{repo.html_url}}
    {{repo.language}}
    {{repo.private}}
  </fieldset>
{{/each}}

{{outlet}}
